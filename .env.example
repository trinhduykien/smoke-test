UAT_EMAIL=dia-chi-email-uat
UAT_PASS=mat-khau-uat
# Tên hiển thị của tài khoản (như app hiện ở góc màn hình, ví dụ: NGUYEN VAN A)
# — dùng cho test menu/fragment. Bỏ trống thì suite vẫn chạy (bỏ qua check tên).
UAT_FULLNAME=

# Test "sai mật khẩu" trong uat-90 chỉ chạy khi đặt QA_WRONG_PW=1
# (mỗi lần chạy ĐÚNG 1 lần nhập sai trên tài khoản của chính bạn —
#  chạy nhiều lần không cần thiết vì có thể khóa tài khoản).
# QA_WRONG_PW=1